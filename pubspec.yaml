name: billi_billi
description: Billi Billi video sharing app
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  video_player: ^2.10.0
  image_picker: ^1.1.2
  camera: ^0.11.0+2
  shared_preferences: ^2.5.3
  path_provider: ^2.1.5
  firebase_core: ^4.14.0
  firebase_auth: ^6.6.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
